/**
 * Movie detail loading - skeleton replica of MovieDetailPage layout.
 */
import { MovieDetailSkeleton } from "@/components/ui/MovieDetailSkeleton";

export default function MovieDetailLoading() {
  return <MovieDetailSkeleton />;
}
